version: '3'

services:
  user:
    image: user:v1
    restart: unless-stopped
    ports:
      - "1100:1100"
    networks:
      - app-network
    depends_on:
      - database
  database:
    image: mongo:4.1.8-xenial
    container_name: mongo
    restart: unless-stopped
    volumes:     
      - dbdata:/data/db
    networks:
      - app-network  

networks:
  app-network:
    driver: bridge

volumes:
  dbdata: 